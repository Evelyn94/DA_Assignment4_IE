1.The IE Assignment written part(Q2-Q4) is written in ANSWERS.PDF.
2.POSTagger.java is used to read in the plain text, then tag it and then write the tag text in the correct format that can be recognized by CRF++.
3.Extractor.java is used to extract the entity from the output file of the CRF test and then output it in the required format.
4.ReadPartOfTrainSens.java is used to spit the esp.train into 1/4, 1/2, 3/4 and prepare them to be used in Q2 for learning curve drawing.
5.template.txt is the template that used in the training process of CRF++.
6.template_explanation.txt is the explanation of template.txt.